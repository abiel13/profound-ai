# ProFund AI — backend config (safe defaults; edit only if you want to customize)
# start.bat / start.sh will auto-generate this file on first run with a random
# JWT_SECRET — you don't need to touch anything.

JWT_SECRET=please-replace-on-first-real-deploy-0000000000000000000000000000
ADMIN_EMAIL=admin@profund.ai
ADMIN_PASSWORD=ProFund2026!
EMERGENT_LLM_KEY=
CORS_ORIGINS=*
